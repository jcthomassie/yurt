---
build:
  - !matrix
      values:
        number: [ 1, 2, 3 ]
        letter: [ a, b, c ]
      include:
      - !link
          target: dir_${{ matrix.letter }}/target_${{ matrix.number }}
          source: dir_${{ matrix.letter }}/source_${{ matrix.number }}
