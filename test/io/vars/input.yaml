---
version: '>=0.5.0'
build:
  - !vars
      alpha: package_2
      beta: package_3
      gamma: package_4
  - !install
    - name: ${{ vars.alpha }}
    - name: ${{ vars.beta }}
    - name: ${{ vars.gamma }}
