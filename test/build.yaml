---
version: 0.1.0
repo:
  local: ~/dotfiles
  remote: https://github.com/user/dotfiles.git
shell: zsh
build:
  # Conditional instructions
  - case:
    - positive:
        spec:
          user: not_user
        build:
        - install:
          - name: not_package
            managers:
            - apt
    - default:
        build:
        # Nested conditional
        - case:
          - positive:
              spec:
                user: not_user
              build:
                - bootstrap:
                  - choco
          - negative:
              spec:
                user: not_user
              build:
                - install:
                  - name: package_0
                    managers:
                      - choco
        - install:
          - name: package_1
            managers:
            - apt
            - apt-get
  # Top level instructions
  - bootstrap:
    - cargo
    - brew
  - link:
    - tail: tail_1
      head: head_1
  - bundle:
      manager: brew
      packages:
      - package_2
      - package_3
      - package_4
