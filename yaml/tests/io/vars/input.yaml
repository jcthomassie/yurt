build:
  - !vars
      alpha: package_2
      beta: package_3
      gamma: package_4
  - !package
      name: ${{ vars.alpha }}
  - !package
      name: ${{ vars.beta }}
  - !package
      name: ${{ vars.gamma }}
